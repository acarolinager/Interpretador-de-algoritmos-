Inicio 
    Real: vInicial, vFinal;
    Caracter: tipo;

    leia(tipo);
    leia(vInicial);

    Se tipo = "1" entao 
        vFinal <- (vInicial * 0.03) + vInicial;
        escreva(vFinal);

   Se tipo = "2" entao
        vFinal <- vInicial * 0.04 + vInicial;
        escreva(vFinal);
Fim
    